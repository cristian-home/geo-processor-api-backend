[project]
name = "geo-processor-api-backend"
version = "0.1.0"
description = "Codebranch geo-processor Api backend (FastAPI) "
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.11.11",
]
